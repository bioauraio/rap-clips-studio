fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
aiosqlite==0.20.0
python-multipart==0.0.9
# [socks] обязателен: до api.anthropic.com мы ходим через мост внешнего
# выхода организма (socks5://172.18.0.1:1081) — напрямую с наших адресов
# приходит 403. Без экстры httpx молча не умеет socks5, и платный канал
# просто не заводится.
httpx[socks]==0.27.2
# Официальный SDK Anthropic для текстовых моделей (backend/textgen.py).
# Свой httpx-клиент он принимает — через него и уходит прокси.
# БЕЗ ЖЁСТКОГО ПИНА СОЗНАТЕЛЬНО: id моделей (claude-opus-5 / claude-sonnet-5)
# и adaptive thinking понимает только свежий SDK, а прошлогодний пин ломает
# канал молча — вызов уходит и возвращает 400 на незнакомую модель.
# Если понадобится воспроизводимость, пин ставить ТОЛЬКО на версию, которую
# перед этим проверили живым вызовом.
anthropic
itsdangerous==2.2.0
# Разбор дорожки в разделе «Аудио» (BPM, доли, секции) — backend/audio_analysis.py.
# ~18 МБ колесо, без numba и scikit-learn: librosa дала бы +400 МБ к образу.
# Модуль переживает отсутствие numpy сам (analysis честно выключается),
# так что откат этой строки сервис не роняет.
numpy==2.1.3
